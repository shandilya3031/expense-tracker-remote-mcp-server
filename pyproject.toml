[project]
name = "test-remote-mcp-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "shandilya3031", email = "tushandilya@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "fastmcp>=3.4.5",
]

[project.scripts]
test-remote-mcp-server = "test_remote_mcp_server:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
